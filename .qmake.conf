load(qt_build_config)

CONFIG += warning_clean c++11
CONFIG += no_testcase_installs
#DEFINES += QT_NO_FOREACH QT_NO_CAST_TO_ASCII QT_NO_CAST_FROM_ASCII

# Warning if we use any feature of Qt which has been marked as deprecated
DEFINES += QT_DEPRECATED_WARNINGS

VIBE_SOURCE_TREE = $$PWD
VIBE_BUILD_DIR = $$shadowed($$PWD)

VIBE_VERSION = 0.9.0
VIBE_VERSION_MAJ = $$section(VIBE_VERSION, ., 0, 0)
VIBE_VERSION_MIN = $$section(VIBE_VERSION, ., 1, 1)
DEFINES += VIBE_VERSION=\\\"$$VIBE_VERSION\\\"

MODULE_VERSION = $$VIBE_VERSION
